import { BondsDirective } from './bonds.directive';

describe('BondsDirective', () => {
  it('should create an instance', () => {
    const directive = new BondsDirective();
    expect(directive).toBeTruthy();
  });
});
