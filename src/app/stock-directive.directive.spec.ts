import { StockDirectiveDirective } from './stock-directive.directive';

describe('StockDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StockDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
